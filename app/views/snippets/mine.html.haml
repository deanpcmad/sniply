- if current_user
	%h2 My Snippets

	%table
		%thead
			%tr
				%th Title
				%th{:style => "width:13%"} Language
				%th{:style => "width:17%"} Created
				%th{:style => "width:17%"}
		%tbody
			- if @snippets.present?
				= render @snippets
			- else
				%tr
					%td{:colspan => "4", :style => "text-align: center; font-style: italic;"} You have no snippets yet!

- else
	%h2{:style => "text-align: center"} #{link_to "Login", root_path} to see you're snippets :)